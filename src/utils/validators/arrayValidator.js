/**
 * Checks if a given array is not empty.
 *
 * @function
 * @name isNotEmptyArray
 * @param {Array} val - The array to check.
 * @returns {boolean} Returns `true` if the array is not empty, `false` otherwise.
 */
export function isNotEmptyArray(val) {
  return val.length > 0;
}
